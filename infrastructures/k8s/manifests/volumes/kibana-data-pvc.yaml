apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kibana-data-pvc
  namespace: staging
  labels:
    app: kibana
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi