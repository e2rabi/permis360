apiVersion: v1
kind: PersistentVolume
metadata:
  name: redis-pv
  namespace: staging
  labels:
    type: local
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
    path: /mnt/data/redis
    type: DirectoryOrCreate
