apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: autoecole-gateway
  namespace: staging
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "13.140.173.47"
        - "*"