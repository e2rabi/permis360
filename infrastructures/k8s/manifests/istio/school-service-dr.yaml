apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: school-service-dr
  namespace: staging
spec:
  host: school-service
  subsets:
    - name: v1
      labels:
        version: v1
      trafficPolicy:
        connectionPool:
          tcp:
            maxConnections: 2